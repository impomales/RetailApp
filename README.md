# RETAIL MEAN STACK APPLICATION

## unhandled cases
- cannot remove item from cart. can only change item quantity to n >= 1
- add button adds multiple instances of same item with quantity one rather than changing quantity.